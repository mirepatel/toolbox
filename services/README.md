Reserved for external API clients (currency rates, background-removal APIs,
etc.) once a tool needs one — keeps fetch/auth logic out of components and
feature folders. Empty for now since no current tool calls an external service.
